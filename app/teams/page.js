export default function page() {
  return (
    <div
      style={{
        color: "var(--subtitle)",
        fontSize: "1.5rem",
        lineHeight: "2rem",
      }}
    >
      Teams page
    </div>
  );
}
